ankiconnect: le cards e manda para o anki
reverso scraper: busca no dicionário
src: principal

Para usar o java scprit Entre no na pasta node_scraper
```bash
    npm install 
    npx playwright install
```
para usar as funções do dicionário entre em run_lookup.js